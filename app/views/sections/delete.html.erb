<% @page_title = "Delete Section: #{@section.name}" %>
<%= link_to("<< Back to Sections List", {:action => 'list', :page_id => @page.id}, :class => 'back-link') %>
<div class="section delete">
  <h2>Delete Section</h2>
  <%= form_for(:section, :url => {:action => 'destroy', :id=> @section.id, :page_id => @page.id}) do |f| %>
      <p class="smallcaps">Are you sure you want to permanently delete this section?</p>

      <p class="reference-name"><%= @section.name %></p>

      <div class="button-container">
        <%= submit_tag("Delete Section") %>
      </div>
      </div>
  <% end %>